# Tuples are immutable, meaning they cannot be changed or modified
numbers = (1, 2, 3, 4, 5)

print(numbers.count(3))  # no of times 3 appears in the tuple
print(numbers.index(3))  # index of 3 in the tuple